
// universal type
export type Code_Type = {
    title: string,
    courseCode?: string,
    language?: string,
    submitAt?: Date,
    code: string,
    isStar?: boolean,
}

// universal type
export type User_Contact_Type = {
    studentProtal?: string,
    telegram?: string,
    github?: string,
    stackOverflow?: string,
    codeForces?: string,
    phone?: string
    address?: string
}